using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SheepSpawner : MonoBehaviour
{
    //WILL SPAWN SHEEP PREFABS. IMPLEMENT AFTER BASE MECHANIC IS WORKING. Have them jump over border fence somehow —
        //maybe an invisible smaller fence within the real one with no collider, AutoMover takes over when reached

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
